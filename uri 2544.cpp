#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;

    while(cin >> n)
    {
        // calculating and displaying the
        //number of times using log2 function
        int a = log2(n);
        cout << a << endl;
    }
    return 0;
}

