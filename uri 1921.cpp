#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long int a,n;

    // input the number of sides
    cin >> n;

    // calculating ...
    a=(n * (n - 3) ) / 2;

    // displaying the number of string
    cout << a << endl;

    return 0;
}

